load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

cc_binary(
    name = "disruptor_queue_benchmark",
    srcs = ["disruptor_queue_benchmark.cpp"],
    deps = [
        "//src:disruptor_queue",
        "@google_benchmark//:benchmark_main",
    ],
)
